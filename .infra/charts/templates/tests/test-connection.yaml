apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "next-tauri-bun.fullname" . }}-test-connection"
  labels:
    {{- include "next-tauri-bun.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "next-tauri-bun.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
